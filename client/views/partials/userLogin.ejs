<div class="login-div formShow">
  <form id="loginForm" action="/user/login" method="POST">
    <div class="sign-group">
      <label for="inputEmail">Email address：</label>
      <input
        class="sign-control"
        type="email"
        id="inputEmail"
        placeholder="Enter email"
        name="email"
        required
      />
    </div>
    <br />
    <div class="sign-group">
      <label for="inputPassword">Password：</label>
      <input
        class="sign-control"
        type="password"
        id="inputPassword"
        placeholder="At least one lower letter and number"
        name="password"
        required
      />
      <div class="showPwBox">
        <input id="showPw" type="checkbox" onclick="myFunction()" />
        <label for="showPw" style="font-size: 1rem">Show Password</label>
      </div>
      <script>
        function myFunction() {
          var pw = document.getElementById("inputPassword");
          if (pw.type == "password") {
            pw.type = "text";
          } else {
            pw.type = "password";
          }
        }
      </script>
    </div>
    <br />
    <button type="submit" class="sign-btn sign-btn-primary">Login</button>
    <button type="click" id="lineBtn" class="sign-btn">Login with LINE</button>
    <p>沒有小時光帳號，我要去 <a class="signin-a">註冊</a></p>
  </form>
</div>
