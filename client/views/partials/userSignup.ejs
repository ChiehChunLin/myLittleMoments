<div class="signup-div formHide">
  <form id="signupForm" action="/user/signup" method="POST">
    <div class="sign-group">
      <label for="inputName">Username：</label>
      <input
        class="sign-control"
        type="text"
        id="myname"
        placeholder="please enter username"
        name="name"
        required
      />
    </div>
    <br />
    <div class="sign-group">
      <label for="inputEmail">Email address：</label>
      <input
        class="sign-control"
        type="email"
        id="inputEmail"
        placeholder="your email"
        name="email"
        required
      />
    </div>
    <br />
    <div class="sign-group">
      <label for="inputSignupPassword">Password：</label>
      <input
        class="sign-control"
        type="password"
        id="inputSignupPassword"
        placeholder="strong password required"
        name="password"
        required
      />
      <div class="showPwBox">
        <input id="showPw" type="checkbox" onclick="mySignupPwFunction()" />
        <label for="showPw" style="font-size: 1rem">Show Password</label>
      </div>
      <script>
        function mySignupPwFunction() {
          var pw = document.getElementById("inputSignupPassword");
          if (pw.type == "password") {
            pw.type = "text";
          } else {
            pw.type = "password";
          }
        }
      </script>
    </div>
    <br />
    <button type="submit" class="sign-btn sign-btn-primary">Submit</button>
    <p>已有小時光帳號，我要去 <a class="login-a">登錄</a></p>
  </form>
</div>
